# === ML PIPELINE REQUIREMENTS ===
# These packages are ONLY for training your models locally.
# The backend also uses scikit-learn, but this file is just for
# running scripts inside the ml_pipeline/ folder.

# pandas reads and manipulates your CSV dataset
pandas==2.2.3

# numpy provides math/array operations
numpy==2.2.6

# scikit-learn gives you all 3 ML algorithms + evaluation tools
scikit-learn==1.6.1

# joblib saves your trained model to a .pkl file
joblib==1.4.2

# matplotlib draws plots (feature importance charts, etc.)
matplotlib==3.10.3

# seaborn makes prettier statistical charts
seaborn==0.13.2

# imbalanced-learn handles class imbalance (more "No" than "Yes" in attrition)
imbalanced-learn==0.14.0

# python-dotenv reads .env files
python-dotenv==1.0.1
