# === WEB FRAMEWORK ===
# FastAPI is the web framework that handles HTTP requests and responses
fastapi==0.111.0

# Uvicorn is the server that actually RUNS your FastAPI app
uvicorn[standard]==0.29.0

# === DATABASE ===
# SQLAlchemy lets Python talk to your PostgreSQL database
sqlalchemy==2.0.30

# asyncpg is the driver that connects SQLAlchemy to PostgreSQL
asyncpg==0.29.0

# psycopg2 is a synchronous PostgreSQL driver (needed for some tools)
psycopg2-binary==2.9.9

# Alembic handles database "migrations" — changes to your DB schema over time
alembic==1.13.1

# === AUTHENTICATION ===
# python-jose creates and verifies JWT (login) tokens
python-jose[cryptography]==3.3.0

# passlib handles password hashing — NEVER store plain passwords
passlib[bcrypt]==1.7.4

# python-multipart lets FastAPI receive form data (like login forms)
python-multipart==0.0.9

# === DATA VALIDATION ===
# Pydantic validates incoming and outgoing JSON data
pydantic==2.7.1

# pydantic-settings loads variables from your .env file
pydantic-settings==2.2.1

# email-validator checks that email addresses are correctly formatted
email-validator==2.1.1

# === MACHINE LEARNING ===
# scikit-learn provides Logistic Regression, Decision Tree, Random Forest
scikit-learn==1.4.2

# pandas handles tabular data (like your CSV dataset)
pandas==2.2.2

# numpy provides math operations used by ML models
numpy==1.26.4

# joblib saves (serializes) your trained ML models to .pkl files
joblib==1.4.2

# === FILE HANDLING ===
# python-dotenv loads your .env file into environment variables
python-dotenv==1.0.1

# aiofiles lets FastAPI read/write files without blocking
aiofiles==23.2.1

# openpyxl lets pandas read Excel files (needed for batch uploads)
openpyxl==3.1.2

# === PDF GENERATION ===
# reportlab creates PDF files from Python
reportlab==4.1.0

# === HTTP CLIENT ===
# httpx is used for making HTTP requests inside your backend (testing, etc.)
httpx==0.27.0

# === CORS ===
# Already included in FastAPI, but listed here for clarity
# Handles Cross-Origin Resource Sharing (lets your React app call your API)
